# Antigravity Dashboard Configuration

# Server Settings
DASHBOARD_PORT=3456
MANAGER_URL=http://localhost:8080

# Database
# DB_PATH=/custom/path/to/usage.db
DATA_RETENTION_DAYS=30
AUTO_CLEANUP_ON_START=false

# Logging
LOG_LEVEL=info

# WebSocket
WS_HEARTBEAT_INTERVAL=30000
WS_MAX_CONNECTIONS=100

# API Rate Limiting (requests per minute)
API_RATE_LIMIT=100

# CORS Origins (comma-separated, defaults to localhost only)
# CORS_ORIGINS=http://localhost:3456,http://localhost:5173

# Development Mode
DEV_MODE=false

# =============================================================================
# Google OAuth Credentials (AUTO-CONFIGURED - DO NOT CHANGE)
# =============================================================================
# These credentials are from the opencode-antigravity-auth plugin.
# Source: https://github.com/NoeFabris/opencode-antigravity-auth/blob/main/src/constants.ts
#
# WARNING: Do NOT use your own OAuth credentials!
# Refresh tokens are bound to the OAuth client that issued them.
# Using different credentials will result in "unauthorized_client" errors.

GOOGLE_CLIENT_ID=1071006060591-tmhssin2h21lcre235vtolojh4g403ep.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=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

# =============================================================================
# OPTIONAL: Dashboard Authentication
# =============================================================================
# When DASHBOARD_SECRET is NOT set:
#   - Server binds to 127.0.0.1 (localhost only)
#   - No authentication required
#   - Safe for local-only use
#
# When DASHBOARD_SECRET IS set:
#   - Server binds to 0.0.0.0 (all interfaces)
#   - All /api/* endpoints require authentication
#   - Use header: Authorization: Bearer <your-secret>
#   - WebSocket: /ws?token=<your-secret>

# DASHBOARD_SECRET=your-secure-random-secret-here
